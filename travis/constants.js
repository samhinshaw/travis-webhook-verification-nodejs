const SCRIPT_NAME = `scripts/deploy-getfit.sh`;
const REPO_NAME = `get_fit`;
const REPO_OWNER = `samhinshaw`;
const REPO_COMPOSE_PATH = `docker-compose.prod.yml`;
const SERVER_DEPLOY_PATH = `/deploy/get_fit`;
const TIMEZONE = `America/Vancouver`;

module.exports = {
  SCRIPT_NAME,
  REPO_NAME,
  REPO_OWNER,
  REPO_COMPOSE_PATH,
  SERVER_DEPLOY_PATH,
  TIMEZONE
};
